import React from 'react';

// import api from '../../services/api';

import { Title } from './style';

const Landing: React.FC = () => {
    return (
        <div>
            <Title color="#00f">Hello World</Title>
        </div>
    );
};

export default Landing;
